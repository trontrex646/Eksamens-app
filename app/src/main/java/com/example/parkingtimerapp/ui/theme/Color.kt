package com.example.parkingtimerapp.ui.theme

import androidx.compose.ui.graphics.Color

// Primary Theme Colors
val DarkBlue = Color(0xFF091626) // Main dark blue
val LightBlue = Color(0xFFE8EFF7) // Light secondary background
val White = Color(0xFFFFFFFF)

// Additional Colors for Depth
val DeepBlue = Color(0xFF1A2B44) // Slightly lighter dark blue
val SoftGray = Color.White // Muted gray
val DarkGray = Color.White // Darker gray for depth
val SoftRed = Color(0xFFFF5252) // Soft red for errors


// Light Theme Colors
val LightBackground = White
val LightSurface = White
val LightOnBackground = DarkBlue
val LightOnSurface = DarkBlue

// Dark Theme Colors
val DarkBackground = Color.Gray
val DarkSurface = DarkBlue
val DarkOnBackground = White
val DarkOnSurface = White
